import { DatabaseController, schema } from "./index.js";
/*
const databaseController = new DatabaseController(DATABASE_HOST, DATABASE_USERNAME, DATABASE_PASSWORD);
const db = databaseController.db;
const sqlOps = databaseController.sqlOps;
*/
export const migrateSpaces = async () => {

}

migrateSpaces();
