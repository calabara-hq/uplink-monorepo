scalar ISODateString
scalar EditorData
scalar IpfsUrl
scalar Decimal




extend type Query {
  generateTwitterDmCode: String!
}

# type Mutation {

# }


extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.0"
    import: ["@key", "@external", "@shareable", "@requires"]
  )
