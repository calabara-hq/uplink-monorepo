extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Space @key(fields: "id") {
  id: ID!
  spaceContests: [Contest]!
}

type Contest {
  id: ID!
  space_id: Int!
  ens: String!
}

extend type Query {
  contests: [Contest!]!
}
