type Space @key(fields: "id") {
  id: ID!
}

type Contest @key(fields: "id") {
  id: ID!
  spaceId: ID!
  tag: String!
  start: String!
  vote: String!
  end: String!
  created: String!
}

type ActiveContest {
  id: ID!
  spaceId: ID!
  tag: String!
  start: String!
  vote: String!
  end: String!
  created: String!
  spaceLink: Space!
}

extend type Query {
  contest(contestId: ID!): Contest!
  spaceContests(spaceId: ID!): [Contest!]!
  activeContests: [ActiveContest!]!
}

extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.0"
    import: ["@key", "@external", "@shareable"]
  )
